name: Refresh Wikipedia localization

on:
  push:
    branches:
      - agent/wikipedia-localization-audit

permissions:
  contents: write

jobs:
  refresh-and-validate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out branch
        uses: actions/checkout@v4
        with:
          ref: agent/wikipedia-localization-audit
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install dependencies
        run: npm install --no-audit --no-fund
      - name: Refresh data and Wikipedia localization
        run: npm run data:update
      - name: Format
        run: npm run format
      - name: Validate
        run: |
          npm run lint
          npm run typecheck
          npm run test
          npm run build
          npm run performance:check
          npm run format:check
      - name: Commit formatted implementation
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package.json scripts/update-wikipedia-localization.mjs
          if git diff --cached --quiet; then
            echo "No implementation changes to commit."
          else
            git commit -m "feat: keep Persian Wikipedia localization current"
            git push origin HEAD:agent/wikipedia-localization-audit
          fi
